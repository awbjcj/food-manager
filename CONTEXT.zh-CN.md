# Food Manager

[English](CONTEXT.md) | [简体中文](CONTEXT.zh-CN.md)

Food Manager 跟踪从购物小票采集到临期提醒的整个食品管理流程。

## 术语

**食品库条目（Pantry Item）**：
来自一行购物小票的一个库存批次，系统将其作为一个整体跟踪和操作。
_避免使用：_ 单份食物、部分消耗的数量

**与食品库相关的收据条目（Pantry-Relevant Receipt Item）**：
购物小票中应当跟踪保质期的已购买食品。
_避免使用：_ 收据行、交易行、会计行

**购买日期（Purchase Date）**：
与食品库相关的收据条目的购买日期。系统会尽可能从小票中读取；无法读取时则假定为扫描日期。
_避免使用：_ 新鲜度日期

**有效食品库条目（Active Pantry Item）**：
仍在食品库中的条目，无论其提醒是否被暂时延后。
_避免使用：_ 未延后提醒的条目

**延后提醒（Snooze）**：
暂时停止为某个有效食品库条目发送提醒。
_避免使用：_ 延长保存期、延长过期日期

**已丢弃食品库条目（Tossed Pantry Item）**：
作为食物浪费而被丢弃的食品库条目。
_避免使用：_ 已删除条目、重复条目

**已移除食品库条目（Removed Pantry Item）**：
因错误记录或重复记录而排除的食品库条目。
_避免使用：_ 已丢弃条目

**保存期（Shelf Life）**：
从保存期起算日期开始，某个食品库条目预计能够保存的天数。
_避免使用：_ 剩余天数、提醒偏移量

**保存期起算日期（Shelf-Life Origin）**：
食品库条目开始计算保存期的日期。
_避免使用：_ 新鲜度日期

**存储状态（Storage State）**：
食品库条目为计算保存期而采用的保存方式：`default`（台面/食品柜）、`fridge`（冷藏）或 `frozen`（冷冻）。状态只能单向前进（`default` → `fridge` → `frozen`），`frozen` 是终止状态。
_避免使用：_ 食品类别、食品库位置

**冷藏食品库条目（Chilled Pantry Item）**：
为了延长保存时间而移入冰箱的食品库条目，其保存期依据冷藏时间，而不是台面或食品柜的默认保存时间。
_避免使用：_ 冷冻条目、已延后提醒的条目

**存储日期（Storage Date）**：
食品库条目进入当前非默认**存储状态**（`fridge` 或 `frozen`）的日期。该术语把过去仅适用于冷冻的日期扩展为通用概念，使所有状态遵循同一条**保存期起算日期**规则。
_避免使用：_ 购买日期、解冻日期、新鲜度日期

**保存期修正（Shelf-Life Correction）**：
用户对某个食品库条目过期日期的修正；它也会让该用户未来对同一标准化食品名称的估算采用修正后的知识。
_避免使用：_ 一次性修改过期日期

## 关系

- 一个**食品库条目**来自一行购物小票。
- **与食品库相关的收据条目**在被记录后成为**食品库条目**。
- 同一张购物小票中的多个相关条目共享一个**购买日期**。
- 如果多个**食品库条目**来自不同的购买，它们可以拥有相同的标准化食品名称。
- 在 v1 中，系统将一个**食品库条目**作为整体食用、丢弃或延后提醒。
- **有效食品库条目**的提醒可以暂停到未来某个日期。
- **延后提醒**不会改变食品库条目的过期日期或保存期。
- 一个**食品库条目**只有一个**存储状态**。
- 只有处于非默认**存储状态**（`fridge` 或 `frozen`）的食品库条目才有**存储日期**。
- **购买日期**是默认的**保存期起算日期**。
- 对处于 `fridge` 或 `frozen` **存储状态**的食品库条目，**存储日期**就是**保存期起算日期**。
- **冷藏食品库条目**仍可移至 `frozen`；移动操作会重置其**存储日期**和**保存期**。
- **保存期**根据食品库条目的**保存期起算日期**决定其过期日期。
- **保存期修正**适用于一个食品库条目，以及该用户未来拥有相同标准化食品名称的食品库条目。
- **已丢弃食品库条目**计入食物浪费；**已移除食品库条目**不计入。

## 对话示例

> **开发者：**“如果购物小票上写着香蕉 x6，点击已食用是否表示只吃了一根香蕉？”
> **领域专家：**“不是。该**食品库条目**被视为一个库存批次，因此整个批次都会离开有效食品库。”

> **开发者：**“如果上一盒牛奶还没喝完又买了一盒，这两条记录应该合并吗？”
> **领域专家：**“不应该。它们是两个独立的**食品库条目**，有各自的购买日期和过期日期。”

> **开发者：**“我们会保存收据折扣、总金额或非食品条目吗？”
> **领域专家：**“不会。只跟踪**与食品库相关的收据条目**。”

> **开发者：**“如果今天扫描一张旧购物小票，食品的保存期会从今天重新开始吗？”
> **领域专家：**“不会。其**保存期**从小票上的**购买日期**开始计算。”

> **开发者：**“如果无法识别小票日期，导入应该停止吗？”
> **领域专家：**“不应该。使用扫描日期作为假定的**购买日期**，并明确显示这一假设。”

> **开发者：**“延后提醒会把**食品库条目**移出食品库吗？”
> **领域专家：**“不会。它仍然是**有效食品库条目**；延后提醒只会暂时将它从提醒中隐藏。”

> **开发者：**“‘两天后提醒’是否表示食物可以多保存两天？”
> **领域专家：**“不是。该**延后提醒**只会推迟提醒时间。”

> **开发者：**“把牛奶修正为 10 天，是指从今天开始算 10 天吗？”
> **领域专家：**“不是。这是从牛奶购买之日起计算的**保存期**。”

> **开发者：**“买鸡肉一周后才将它冷冻，冷冻保存期会从小票日期开始计算吗？”
> **领域专家：**“不会。**冷冻日期**会成为新的**保存期起算日期**。”

> **开发者：**“如果修正过一次草莓，未来的草莓也应该使用这个值吗？”
> **领域专家：**“是的。**保存期修正**会更新该标准化食品名称的估算知识。”

> **开发者：**“如果一个条目被导入了两次，应该把它标记为已丢弃吗？”
> **领域专家：**“不应该。应将它设为**已移除食品库条目**，以保证浪费统计准确。”

## 已标记的模糊点

- v1 明确不支持部分消耗一个**食品库条目**。
- 收据会计信息和收据行归档不在范围内；仅跟踪**与食品库相关的收据条目**。
- 延后提醒不会创建独立的食品库条目状态；它只会暂时停止为**有效食品库条目**发送提醒。
- **延后提醒**不会延长**保存期**。
- 修正使用从**保存期起算日期**计算的**保存期**，而不是剩余天数。
- 在 v1 中，**保存期修正**的作用范围有意大于一次性修改过期日期。
- **已丢弃食品库条目**专指食物浪费；清理错误导入时使用**已移除食品库条目**。
